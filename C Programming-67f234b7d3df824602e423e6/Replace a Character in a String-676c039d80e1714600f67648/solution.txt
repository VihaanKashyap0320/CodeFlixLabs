[{"code":"#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char str[100];\n    char oldChar, newChar;\n\n    // Input string\n    scanf(\"%[^\\n]\", str);\n    getchar(); // to consume the newline character\n\n    // Input character to replace\n    scanf(\"%c\", &oldChar);\n    getchar(); // to consume the newline character\n\n    // Input replacement character\n    scanf(\"%c\", &newChar);\n\n    int len = strlen(str);\n\n    for (int i = 0; i < len; i++) {\n        if (str[i] == oldChar) {\n            str[i] = newChar;\n        }\n    }\n\n    // Output the modified string\n    printf(\"%s\\n\", str);\n\n    return 0;\n}","language":"c","time":"2025-05-04T09:41:13.562Z","isCorrect":true},{"code":"#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char str[100];\n    char oldChar, newChar;\n\n    // Input string\n    scanf(\"%[^\\n]\", str);\n    getchar(); // to consume the newline character\n\n    // Input character to replace\n    scanf(\"%c\", &oldChar);\n    getchar(); // to consume the newline character\n    // Input replacement character\n    scanf(\"%c\", &newChar);\n    int len = strlen(str);\n\n    for (int i = 0; i < len; i++) {\n        if (str[i] == oldChar) {\n            str[i] = newChar;\n        }\n    }\n\n    // Output the modified string\n    printf(\"%s\\n\", str);\n\n    return 0;\n}","language":"c","time":"2025-05-04T09:41:14.659Z","isCorrect":true},{"code":"#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char str[100];\n    char oldChar, newChar;\n\n    // Input string\n    scanf(\"%[^\\n]\", str);\n    getchar(); // to consume the newline character\n\n    // Input character to replace\n    scanf(\"%c\", &oldChar);\n    getchar(); // to consume the newline character\n    // Input replacement character\n    scanf(\"%c\", &newChar);\n    int len = strlen(str);\n    for (int i = 0; i < len; i++) {\n        if (str[i] == oldChar) {\n            str[i] = newChar;\n        }\n    }\n\n    // Output the modified string\n    printf(\"%s\\n\", str);\n\n    return 0;\n}","language":"c","time":"2025-05-04T09:41:16.876Z","isCorrect":true},{"code":"#include <stdio.h>\n#include <string.h>\nint main() {\n    char str[100];\n    char oldChar, newChar;\n    scanf(\"%[^\\n]\", str);\n    // getchar(); // to consume the newline character\n    // Input character to replace\n    scanf(\"%c\", &oldChar);\n    // getchar(); // to consume the newline character\n    // Input replacement character\n    scanf(\"%c\", &newChar);\n    int len = strlen(str);\n    for (int i = 0; i < len; i++) {\n        if (str[i] == oldChar) {\n            str[i] = newChar;\n        }\n    }\n    printf(\"%s\\n\", str);\n    return 0;\n}","language":"c","time":"2025-05-04T09:41:55.132Z","isCorrect":false}]