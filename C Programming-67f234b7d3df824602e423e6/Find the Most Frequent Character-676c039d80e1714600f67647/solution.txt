[{"code":"#include <stdio.h>\n\nint main() {\n    char str[1000];\n    int i, j, count, max = 0;\n    char result = 'z' + 1;\n\n    // Read input string\n    gets(str);  // simple input\n\n    // Loop through each character\n    for (i = 0; str[i] != '\\0'; i++) {\n        char ch1 = str[i];\n\n        // Convert uppercase to lowercase manually\n        if (ch1 >= 'A' && ch1 <= 'Z') {\n            ch1 = ch1 + ('a' - 'A');\n        }\n\n        // Skip non-alphabet characters\n        if (!(ch1 >= 'a' && ch1 <= 'z')) {\n            continue;\n        }\n\n        count = 0;\n\n        // Count how many times this character appears\n        for (j = 0; str[j] != '\\0'; j++) {\n            char ch2 = str[j];\n\n            if (ch2 >= 'A' && ch2 <= 'Z') {\n                ch2 = ch2 + ('a' - 'A');\n            }\n\n            if (ch2 == ch1) {\n                count++;\n            }\n        }\n\n        // Update result if this character has higher or equal frequency\n        if (count > max || (count == max && ch1 < result)) {\n            max = count;\n            result = ch1;\n        }\n    }\n\n    printf(\"%c\\n\", result);\n    return 0;\n}","language":"c","time":"2025-05-04T07:52:32.825Z","isCorrect":true},{"code":"#include <stdio.h>\nint main() {\n    char str[1000];\n    int i, j, count, max = 0;\n    char result = 'z' + 1;\n\n    // Read input string\n    gets(str);  // simple input\n\n    // Loop through each character\n    for (i = 0; str[i] != '\\0'; i++) {\n        char ch1 = str[i];\n\n        // Convert uppercase to lowercase manually\n        if (ch1 >= 'A' && ch1 <= 'Z') {\n            ch1 = ch1 + ('a' - 'A');\n        }\n\n        // Skip non-alphabet characters\n        if (!(ch1 >= 'a' && ch1 <= 'z')) {\n            continue;\n        }\n\n        count = 0;\n\n        // Count how many times this character appears\n        for (j = 0; str[j] != '\\0'; j++) {\n            char ch2 = str[j];\n\n            if (ch2 >= 'A' && ch2 <= 'Z') {\n                ch2 = ch2 + ('a' - 'A');\n            }\n\n            if (ch2 == ch1) {\n                count++;\n            }\n        }\n\n        // Update result if this character has higher or equal frequency\n        if (count > max || (count == max && ch1 < result)) {\n            max = count;\n            result = ch1;\n        }\n    }\n\n    printf(\"%c\\n\", result);\n    return 0;\n}","language":"c","time":"2025-05-04T07:52:34.131Z","isCorrect":true},{"code":"#include <stdio.h>\nint main() {\n    char str[1000];\n    int i, j, count, max = 0;\n    char result = 'z' + 1;\n    // Read input string\n    gets(str);  // simple input\n\n    // Loop through each character\n    for (i = 0; str[i] != '\\0'; i++) {\n        char ch1 = str[i];\n\n        // Convert uppercase to lowercase manually\n        if (ch1 >= 'A' && ch1 <= 'Z') {\n            ch1 = ch1 + ('a' - 'A');\n        }\n\n        // Skip non-alphabet characters\n        if (!(ch1 >= 'a' && ch1 <= 'z')) {\n            continue;\n        }\n\n        count = 0;\n\n        // Count how many times this character appears\n        for (j = 0; str[j] != '\\0'; j++) {\n            char ch2 = str[j];\n\n            if (ch2 >= 'A' && ch2 <= 'Z') {\n                ch2 = ch2 + ('a' - 'A');\n            }\n\n            if (ch2 == ch1) {\n                count++;\n            }\n        }\n\n        // Update result if this character has higher or equal frequency\n        if (count > max || (count == max && ch1 < result)) {\n            max = count;\n            result = ch1;\n        }\n    }\n\n    printf(\"%c\\n\", result);\n    return 0;\n}","language":"c","time":"2025-05-04T07:52:35.964Z","isCorrect":true},{"code":"#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char str[100];\n    gets(str);  // Read input string\n\n    int max = 0;\n    char result = 127; // Start with highest printable ASCII\n\n    int len = strlen(str);\n\n    for (int i = 0; i < len; i++) {\n        char ch1 = str[i];\n        if (ch1 == ' ') continue; // Skip spaces\n\n        int count = 0;\n        for (int j = 0; j < len; j++) {\n            if (str[j] == ch1)\n                count++;\n        }\n\n        if (count > max || (count == max && ch1 < result)) {\n            max = count;\n            result = ch1;\n        }\n    }\n\n    printf(\"%c\\n\", result);\n    return 0;\n}","language":"c","time":"2025-05-04T08:57:28.135Z","isCorrect":true}]